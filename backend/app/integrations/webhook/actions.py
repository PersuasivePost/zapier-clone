"""
Webhook Integration - Actions

Webhook integration has NO actions in the MVP.

Why? Because webhook is fundamentally about RECEIVING data (trigger),
not SENDING data (action).

Future considerations:
- Could add a "Send Webhook" action for POSTing to external URLs
- But that's not core to the webhook integration concept
- If needed, create a separate "HTTP" or "REST API" integration for that

For now: empty actions list.
"""

# No actions for webhook integration
# This file exists for consistency in folder structure
